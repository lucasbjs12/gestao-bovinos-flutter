{
  "name": "tests-rules",
  "description": "Testes das security rules do Firestore (emulador)",
  "private": true,
  "scripts": {
    "test": "mocha rules.test.mjs --timeout 15000"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "mocha": "^11.0.0"
  }
}
